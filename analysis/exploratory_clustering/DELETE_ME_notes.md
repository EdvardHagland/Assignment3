# DELETE ME Notes

This is a temporary scratch file for early ideas.

Questions for the first exploratory pass:

- Do `pre_2022` and `post_2022` rows distribute differently across clusters?
- Are the strongest differences driven by a few firms or shared across the corpus?
- Do emergent clusters resemble our eventual hand-labeled categories?
- Are there obvious "event" clusters that come from one-off acquisitions or specific corporate events rather than broader sector change?

Things to watch for:

- acquisition-heavy rows, especially in smaller firms
- repeated boilerplate that might dominate a cluster
- clusters that are really just style or format clusters instead of substantive risk themes
- whether bullet-split rows behave differently from other merge types
