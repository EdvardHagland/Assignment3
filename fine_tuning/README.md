# Fine-Tuning

This folder contains the supervised-labeling workflow.

Most of the operational labeling system now lives in:

- `fine_tuning/labeling/`

That subfolder contains:

- the annotation protocol
- the codebook
- the label template
- the app config
- the webapp
- the helper scripts

The actual annotation database and exported label artifacts still live in `data/fine_tuning/`.
